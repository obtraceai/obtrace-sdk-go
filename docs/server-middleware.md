# Server Middleware

Available middleware adapters:
- `middleware/nethttp`
- `middleware/gin`
- `middleware/echo`

These wrappers log request telemetry while keeping business logic on the server app side.
